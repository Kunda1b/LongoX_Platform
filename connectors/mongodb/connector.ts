export { actions } from './actions';
export { triggers } from './triggers';
export { auth } from './auth';
export { schemas } from './schemas';
export const connector = { name: 'mongodb', version: '1.0.0', auth: ['api_key'], actions: [], triggers: [] };
