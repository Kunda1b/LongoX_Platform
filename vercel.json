{
  "installCommand": "pnpm install --frozen-lockfile",
  "experimentalServices": {
    "web": {
      "root": "apps/web",
      "buildCommand": "pnpm --filter @longox/web run build",
      "framework": "nextjs",
      "routePrefix": "/"
    }
  }
}
