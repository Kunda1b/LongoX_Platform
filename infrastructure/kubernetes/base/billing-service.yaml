apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: platform-services
  labels:
    app: billing-service
    app.kubernetes.io/name: billing-service
    app.kubernetes.io/part-of: longox
spec:
  replicas: 2
  selector:
    matchLabels:
      app: billing-service
  template:
    metadata:
      labels:
        app: billing-service
    spec:
      containers:
        - name: billing-service
          image: longox/billing-service:latest
          ports:
            - containerPort: 3004
          env:
            - name: PORT
              value: "3004"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: longox-secrets
                  key: database-url
            - name: NATS_URL
              value: "nats://nats-platform:4222"
            - name: EVENT_BUS_TYPE
              value: "nats"
            - name: EVENT_BUS_URL
              value: "nats://nats-platform:4222"
            - name: STRIPE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: longox-secrets
                  key: stripe-secret-key
                  optional: true
            - name: STRIPE_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: longox-secrets
                  key: stripe-webhook-secret
                  optional: true
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3004
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/ready
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: platform-services
spec:
  selector:
    app: billing-service
  ports:
    - port: 3004
      targetPort: 3004
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: billing-service-hpa
  namespace: platform-services
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: billing-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
