apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.workflowService.name }}
  labels:
    app: {{ .Values.workflowService.name }}
    {{- include "longox.labels" $ | nindent 4 }}
spec:
  replicas: {{ .Values.workflowService.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.workflowService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.workflowService.name }}
    spec:
      containers:
        - name: {{ .Values.workflowService.name }}
          image: "{{ .Values.workflowService.image.repository }}:{{ .Values.workflowService.image.tag }}"
          imagePullPolicy: {{ .Values.workflowService.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.workflowService.service.targetPort }}
          env:
            {{- range $key, $value := .Values.workflowService.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: database_url
          resources:
            {{- toYaml .Values.workflowService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /api/health
              port: {{ .Values.workflowService.service.targetPort }}
          readinessProbe:
            httpGet:
              path: /api/health
              port: {{ .Values.workflowService.service.targetPort }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.workflowService.name }}
  labels:
    app: {{ .Values.workflowService.name }}
spec:
  type: {{ .Values.workflowService.service.type }}
  ports:
    - port: {{ .Values.workflowService.service.port }}
      targetPort: {{ .Values.workflowService.service.targetPort }}
      protocol: TCP
  selector:
    app: {{ .Values.workflowService.name }}
