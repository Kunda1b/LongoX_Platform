apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.schedulerService.name }}
  labels:
    app: {{ .Values.schedulerService.name }}
    {{- include "longox.labels" $ | nindent 4 }}
spec:
  replicas: {{ .Values.schedulerService.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.schedulerService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.schedulerService.name }}
    spec:
      containers:
        - name: {{ .Values.schedulerService.name }}
          image: "{{ .Values.schedulerService.image.repository }}:{{ .Values.schedulerService.image.tag }}"
          imagePullPolicy: {{ .Values.schedulerService.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.schedulerService.service.targetPort }}
          env:
            {{- range $key, $value := .Values.schedulerService.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: database_url
          resources:
            {{- toYaml .Values.schedulerService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /api/health
              port: {{ .Values.schedulerService.service.targetPort }}
          readinessProbe:
            httpGet:
              path: /api/health
              port: {{ .Values.schedulerService.service.targetPort }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.schedulerService.name }}
  labels:
    app: {{ .Values.schedulerService.name }}
spec:
  type: {{ .Values.schedulerService.service.type }}
  ports:
    - port: {{ .Values.schedulerService.service.port }}
      targetPort: {{ .Values.schedulerService.service.targetPort }}
      protocol: TCP
  selector:
    app: {{ .Values.schedulerService.name }}
