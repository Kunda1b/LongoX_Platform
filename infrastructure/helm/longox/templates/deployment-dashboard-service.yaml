apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.dashboardService.name }}
  labels:
    app: {{ .Values.dashboardService.name }}
    {{- include "longox.labels" $ | nindent 4 }}
spec:
  replicas: {{ .Values.dashboardService.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.dashboardService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.dashboardService.name }}
    spec:
      containers:
        - name: {{ .Values.dashboardService.name }}
          image: "{{ .Values.dashboardService.image.repository }}:{{ .Values.dashboardService.image.tag }}"
          imagePullPolicy: {{ .Values.dashboardService.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.dashboardService.service.targetPort }}
          env:
            {{- range $key, $value := .Values.dashboardService.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: database_url
          resources:
            {{- toYaml .Values.dashboardService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /api/health
              port: {{ .Values.dashboardService.service.targetPort }}
          readinessProbe:
            httpGet:
              path: /api/health
              port: {{ .Values.dashboardService.service.targetPort }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.dashboardService.name }}
  labels:
    app: {{ .Values.dashboardService.name }}
spec:
  type: {{ .Values.dashboardService.service.type }}
  ports:
    - port: {{ .Values.dashboardService.service.port }}
      targetPort: {{ .Values.dashboardService.service.targetPort }}
      protocol: TCP
  selector:
    app: {{ .Values.dashboardService.name }}
